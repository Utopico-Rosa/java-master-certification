/** 
 * Provides the integration functionality of the program. It consists of
 * delegate/controller classes that take requests from the graphical user
 * interface classes, transforms those requests and passes them along to the
 * business logic rules classes. Then it receives the answers from the business 
 * logic rules classes, transforms those responses and passes them along to the
 * graphical user interface classes. 
 */
package suncertify.integration;
