/** 
 * Contains the graphical user interfaces that allow the user(s) to configurate
 * the remote server, the local non-distributed client and the networked
 * distributed client. Moreover, it contains the parent class that shows the
 * database data and has "template methods" that calls on fine-grained
 * operations that are implemented by child classes, whether the operation is
 * local or network distributed.
 */
package suncertify.presentation;
